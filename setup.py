from setuptools import setup, find_packages

setup(
    name='jogovelha',
    version='1.0',
    description='Pacote que contém o jogo da velha',
    install_requires=['numpy'],
    packages=find_packages()
)